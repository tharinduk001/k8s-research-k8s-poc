apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-pvc
  namespace: student-management
  labels:
    app: database
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
