apiVersion: v1
kind: ConfigMap
metadata:
  name: database-init
  namespace: student-management
  labels:
    app: database
data:
  init.sql: |
    CREATE TABLE IF NOT EXISTS students (
        id SERIAL PRIMARY KEY,
        name VARCHAR(100),
        email VARCHAR(100),
        course VARCHAR(100)
    );
